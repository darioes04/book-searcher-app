export interface Book {

    id: string,
    title: string,
    author: string,
    publisher: string,
    publishedDate: string,
    pageNumber: number,
    description: string,
    thumbnail: string;
    isbn: string;
    
}